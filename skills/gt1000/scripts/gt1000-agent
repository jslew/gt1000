#!/usr/bin/env bash
set -euo pipefail

SKILL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONDONTWRITEBYTECODE=1
export PYTHONPATH="${SKILL_DIR}/tools${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -B "$SKILL_DIR/tools/gt1000/agent_cli.py" "$@"
